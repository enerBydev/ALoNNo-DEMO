// Esqueleto de methodOS: existe para que el proyecto NAZCA con `just ci` en verde.
// Sustitúyelo por pruebas reales; no lo borres sin dejar otras (TEST-1).
import { describe, expect, it } from 'vitest'

describe('esqueleto', () => {
  it('respira', () => {
    expect(1 + 1).toBe(2)
  })
})
