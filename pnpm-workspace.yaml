allowBuilds:
  esbuild: true
  # `workerd` es el runtime de Workers y entra por wrangler. Sin permiso, `pnpm install`
  # sale 1 con ERR_PNPM_IGNORED_BUILDS y tumba el gate entero.
  workerd: true

catalog:
  wrangler: 4.129.0
