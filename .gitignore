# Documentacion y material de negocio del proyecto: viven en un repo aparte y
# PRIVADO, clonado aqui como docs/. Este repo es PUBLICO: nunca versionar docs/.
docs/

# Symlink a docs/CLAUDE.md, para que las reglas del proyecto se carguen solas
# al abrir sesion. Apunta al repo privado, asi que no viaja con este repo.
/CLAUDE.md

# --- andamiaje de methodOS (plantilla node) ---
.direnv/
.env
.env.*
a.out
*.o
node_modules/
.nuxt/
.output/
dist/
.wrangler/
.data/
.dev.vars
supabase/.temp/
__pycache__/

# Ficheros locales y temporales del harness de Claude Code: son de ESTA maquina,
# no del proyecto. Uno se colo en un commit el 2026-09-09 via `git add -A`.
.claude/settings.local.json
.claude/settings.local.json.tmp.*

# El CI clona el kit de methodOS DENTRO del arbol (`path: .methodos`) porque `uses: ./...`
# lo exige. No es codigo de este repo y no se versiona.
.methodos/
